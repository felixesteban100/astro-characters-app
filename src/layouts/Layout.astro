---
import "../styles/globals.css";
import { ViewTransitions } from "astro:transitions";
// import { fade } from "astro:transitions";
import NavLinks from "src/components/NavLinks.astro";
import Sheet from "src/components/Sheet.svelte";

// import * as Sheet from "$lib/components/ui/sheet";
import { Menu } from "lucide-astro";
// import Sparkles from "src/components/Sparkles.astro";
import Sparkles from "src/components/Sparkles.svelte";


// things to fix:
/* 
- use astro-particles
- dark mode toggling when changes page while using viewTransition (in the mean time I have just dark mode by default)
- create favorites page
CharacterFeatures/Tabs (shadcn-ui) component works at times??? :-/
discover a way to present webp images  
*/

interface Props {
  title: string;
  otherLink?: {
    url: string;
    title: string;
  };
}

const { title, otherLink } = Astro.props;

const propsParticles = {
  id: "tsparticlesfullpage",
  background: "transparent",
  minSize: 0.6,
  maxSize: 1.4,
  particleDensity: 50,
  particleColor: "#000000",
};

const propsParticlesDark = {
  ...propsParticles,
  particleColor: "#fff",
  id: "tsparticlesfullpage2",
};
---

<!-- <script is:inline>
  const getThemePreference = () => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches
      ? "dark"
      : "light";
  };
  const isDark = getThemePreference() === "dark";
  document.documentElement.classList[isDark ? "add" : "remove"]("dark");

  if (typeof localStorage !== "undefined") {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["class"],
    });
  }
</script> -->
<html lang="en">
  <!-- the following line doesn't work, so I don't even bother using it -->
  <!-- transition:animate={fade({ duration: "15s" })} -->
  <head  >
    <ViewTransitions />
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <div class="absolute inset-0 -z-10 min-h-screen">
      <!-- <Sparkles/> -->
      <Sparkles client:only/>
    </div>
    
    <main class="w-[90vw] md:w-[80vw] mx-auto">
      <div
        class="flex items-center justify-between md:justify-start mx-auto py-4 gap-5"
      >
        <a href="/"
          ><p class="text-primary font-bold text-stroke-custom">ASTRO 🚀</p></a
        >
        <NavLinks
          otherClassesContainer="hidden md:flex"
          title={title}
          otherLink={otherLink}
        />
        <Sheet ButtonClassName="flex md:hidden" client:load>
          <Menu slot="trigger" className="text-foreground" />
          <NavLinks
            slot="content"
            otherClassesContainer="flex"
            title={title}
            otherLink={otherLink}
          />
        </Sheet>
      </div>
      <div class="mx-auto">
        <slot />
      </div>
    </main>
  </body>
</html>
