---
import "@/styles/globals.css";
import { ViewTransitions } from "astro:transitions";
// import { ModeToggle } from "@/components/mode-toogle";
import { SparklesCore } from "@/components/ui/sparkles";
import { fade } from "astro:transitions";
import NavLinks from "@/components/NavLinks.astro";
import SheetNav from "@/components/SheetNav";

// things to fix:
/* 
- dark mode toggling when changes page while using viewTransition (in the mean time I have just dark mode by default)
- create favorites page
CharacterFeatures/Tabs (shadcn-ui) component works at times??? :-/
make team change or appear when universe changes not when url is sended (in characters.astro form)
discover a way to present webp images  
*/

interface Props {
  title: string;
  otherLink?: {
    url: string;
    title: string;
  };
}

const { title, otherLink } = Astro.props;

const propsParticles = {
  id: "tsparticlesfullpage",
  background: "transparent",
  minSize: 0.6,
  maxSize: 1.4,
  particleDensity: 50,
  particleColor: "#000000",
};

const propsParticlesDark = {
  ...propsParticles,
  particleColor: "#fff",
  id: "tsparticlesfullpage2",
};
---

<!-- <script is:inline>
  const getThemePreference = () => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches
      ? "dark"
      : "light";
  };
  const isDark = getThemePreference() === "dark";
  document.documentElement.classList[isDark ? "add" : "remove"]("dark");

  if (typeof localStorage !== "undefined") {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["class"],
    });
  }
</script> -->
<html lang="en">
  <head transition:animate={fade({ duration: "15s" })}>
    <ViewTransitions />
    <meta charset="UTF-8" />
    <meta name="description" content="Astro description" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <div class="absolute inset-0 -z-10 min-h-screen">
      <SparklesCore transition:persist client:visible {...propsParticles} />
      <SparklesCore transition:persist client:visible {...propsParticlesDark} />
    </div>
    <main class="w-[80vw] mx-auto">
      <!-- <nav class="bg-background w-full border-b border-foreground/20 mb-5">
        
      </nav> -->
      <div
        class="flex items-center justify-between md:justify-start mx-auto py-4 gap-5"
      >
        <a href="/"
          ><p class="text-primary font-bold text-stroke-custom">ASTRO 🚀</p></a
        >
        <NavLinks
          otherClassesContainer="hidden md:flex"
          title={title}
          otherLink={otherLink}
        />
        <SheetNav client:load>
          <NavLinks
            otherClassesContainer="flex"
            title={title}
            otherLink={otherLink}
          />
        </SheetNav>
        <!-- <ModeToggle client:load /> -->
      </div>
      <div class="mx-auto">
        <slot />
      </div>
    </main>
  </body>
</html>
